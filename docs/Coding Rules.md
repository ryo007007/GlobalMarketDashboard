# Coding Rules（コーディング規則）

コードの視認性・保守性を高め、将来的にプログラムが大規模になっても構造をわかりやすく保つための規則です。

---

## 1. 変数の命名規則（Prefix）

変数の役割やスコープが一目でわかるように、接頭辞（プレフィックス）を付与します。

* `g_` : グローバル変数（Global）
* `m_` : メンバー変数 / クラス変数（Member）
* `Inp` : ユーザー設定・入力パラメータ（Input）

---

## 2. 関数の命名規則（動詞プレフィックス）

関数の処理内容に応じて、先頭に付ける動詞を統一します。

* `Draw...` : チャートへの描画・表示処理
* `Update...` : データや表示の更新・再計算処理
* `Detect...` : 条件判定・シグナル検知処理
* `Calc...` : 数値やインジケーターの計算処理

---

## 3. クラスの命名規則

クラス名には頭文字に **`C`** を付け、PascalCase（単語の区切りを大文字）で記述します。

* `CCurrencyStrength`
* `CMoneyFlow`
